# 脱敏说明（SANITIZE-NOTES）

本文件记录本仓库从**个人博客真源**到**开源模板**的脱敏范围、方法与残留边界，供维护者复核。

脱敏对象：本仓库由私人博客仓库复制而来，原仓库包含真实域名、服务器地址、SSH 身份、私人远端
仓库地址、站点所有者资质信息（备案号）、本机绝对路径与个人文章正文。本仓库是这些信息被替换或
移除后的模板版本。

## 1. 替换规则

| 类别 | 原值（示例形式） | 本仓库 |
| --- | --- | --- |
| 站点域名 | 真实个人域名 | `example.com`（RFC 2606 保留域） |
| 服务器地址 | 真实公网 IP | `203.0.113.10`（RFC 5737 TEST-NET-3） |
| SSH 私钥名 | `<所有者>_ed25519` | `example_ed25519` |
| 部署根 / 数据目录 | `/srv/<所有者>-blog*` | `/srv/example-blog*` |
| systemd unit | `<所有者>-auth` / `<所有者>-health` | `example-auth` / `example-health` |
| 启动身份品牌 | 站点所有者姓名 | `JOYCE MOORE` / `Joyce`（取自上游 RhineLabUI 的既有身份图形） |
| 私人仓库 | `<用户>/<仓库>` | `example-org/example-blog` |
| Go 模块路径 | 私人仓库路径 | `github.com/example-org/example-blog/services/lab-auth` |
| 备案信息 | 真实 ICP / 公安备案号与图标 | **整体移除**，页脚留有按需填写的注释 |
| 本机路径 | `C:/Users/<所有者>/…`、个人项目绝对路径 | 相对路径或 `C:/Users/example/…` |
| 文章作者 | 站点所有者账号名 | `Example Author` |
| 站名 | 个人博客名 | `示例博客` |

替换使用字面量规则（不用正则通配），因此不会误伤上游署名与第三方许可文本。

## 2. 移除的内容

- **旧站点迁移相关内容**：迁移清单与对账产物、迁移/对账/重定向/记录导出脚本、旧站备份脚本、
  旧站演示夹具（`content/archives.json` 与 `public/archives/`）以及迁移阶段报告归档。
- **旧站真实运行数据**：一份记录旧站启动动画逐帧状态的历史夹具（含旧站身份与账号名）。
- **第三方个人素材**：一张第三方动漫形象头像，已替换为同尺寸的中性直角占位图
  （`demo-responsive-placeholder*.png`，由本仓库脚本生成）。
- **备案图标**：随备案号一并移除。
- **开发过程文档**：真源仓库里的计划、门报告、评估与交接记录不随模板分发；其中仍然有效的结论与
  参数已**总结为操作与参考手册**：[docs/READER.md](docs/READER.md)（阅读层契约与参数）、
  [docs/IDENTITY.md](docs/IDENTITY.md)（身份与认证规则）、[docs/UPSTREAM.md](docs/UPSTREAM.md)
  （来源与许可）、[docs/BUILD.md](docs/BUILD.md) 与 [docs/AUTHORING.md](docs/AUTHORING.md)（操作说明）。
- **过时说明**：已废弃方案（不再使用的字体评估等）与指向已删产物的引用一并清理；
  仍依赖被删路径的两处脚本（README 截图输出目录、配对打包的历史门报告字段）已同步调整。

站内示例文章已改写为中性内容，但**保留**原有稳定 `id` 与 `legacyUrls`（用于演示重定向字段），
规范路径改为中性英文 slug。

## 3. 残留与边界（重要）

请在转公开前自行确认以下事项，本仓库不做承诺：

1. **历史开发文档已总结为手册**：真源仓库的开发过程记录（方案、门报告、评估、交接记录）不随模板
   分发；其中仍然有效的结论、契约与参数已整理进 [docs/](docs/README.md)（操作与参考手册）、
   [DESIGN.md](DESIGN.md) 与 [BLOG-MAINTAIN-PERFECT.md](BLOG-MAINTAIN-PERFECT.md)。因此仓库里
   不存在会被误读为当前事实的阶段性表述；需要过程记录时请自行在私有仓库维护。
2. **第三方素材的授权范围未变**：三维模型、图像、动图、字体与音频仍受各自许可约束，MIT 不覆盖
   它们。详见 [README 的「来源与许可」](README.md) 与 `public/` 下各许可证文件。
3. **`art/` 与 `reference/`** 内的 Blender 工程与对照页可能包含原始作者的路径与备注，属于上游
   素材的一部分，未做改写。
4. **文档中的示例主机与账号**（`root@203.0.113.10`、`/srv/example-blog`）是模板占位值，直接
   照抄不会连到任何真实主机；部署前必须替换成自己的环境。
5. 本仓库的 git 历史是**全新历史（单个初始提交）**，不含真源仓库的提交记录，因此不存在旧提交里
   残留敏感信息的风险。

## 4. 复核方式

```bash
# 敏感串复检（应无命中，示例：真实域名/主机/账号关键词）
rg -n "<真实域名>|<真实IP>|<所有者姓名>|beian|备案号" --glob '!node_modules'

# 内容契约与构建
npm ci --ignore-scripts
npm run check:content
npm run build
```
